<% unless flash.empty? %>
  <script type='text/javascript'>
    <% flash.each do |f| %>
    <% type = f[0].to_s %>
    <% message = f[1].to_s %>
    toastr['<%= type %>']('<%= message %>', 'Authors Haven');
    <% end %>
  </script>
<% end %>