<div class="container-fluid tab">
    <ul class="nav container d-flex flex-row">
        <li class="nav-item">
            <%= link_to "#{@articles.count} Posts", profile_path(current_user.first_name), class: "nav-link current" %>
        </li>
        <li class="nav-item">
            <%= link_to "#{@followers.count} Followers", followers_path(current_user.first_name), class: "nav-link text-white" %>
        </li>
        <li class="nav-item">
            <%= link_to "#{@following.count} Following", following_path(current_user.first_name), class: "nav-link text-white" %>
        </li>
    </ul>
</div>
